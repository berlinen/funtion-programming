// 默认导出

// 每个模块都可以选择导出default导出。默认导出用关键字标记default; 并且default每个模块只能有一个导出。 default导出使用不同的导入形式导入。

// declare let $: JQuery;
// export default $;

// 引入

// import $ from "JQuery";

// $("button.continue").html( "Next Step..." );
